接口自动化框架(1)：
    1、获取用例
    2、调用接口
    3、校验结果的
    4、发送测试报告
    5、异常处理
    6、日志

目录介绍：
    bin：        运行脚本目录
    cases:      存放测试用例目录
    conf:       存放配置类相关(定义项目中使用到的路径)
    lib:        依赖的相关工具包
    logs:       项目日志


1、通过start.py文件启动测试
2、通过setting配置的用例所在路径
3、遍历读取该路径下的所有测试用例文件
4、通过读取测试用例文件中的相关接口信息
5、通过测试用例中的接口信息,请求接口,拿到返回数据对比验证
6、统计验证通过和失败的用例,并记录到excel文件中,发送右键给相应负责人
